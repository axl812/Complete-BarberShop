<h2>Наши контакты</h2>

<p>Обратная связь</p>

<form action="/contacts" method="POST">

  <div class="form-group">
    <label>Ваш E-mail (мы Вам обязательно ответим):</label>
    <input type="text" name='mail' class="form-control" placeholder="Введите Ваш e-mail" value="<%= @mail %>">
  </div>

  <div class="form-group">
    <label>Ваше сообщение</label>
    <textarea type="text" name='message_from_mail' class="form-control" rows="5" placeholder="Текст сообщения" value="<%= @message_from_mail %>"><%= @message_from_mail %></textarea>
  </div>

  <button type="submit" class="btn btn-primary">Отправить!</button>

</form>